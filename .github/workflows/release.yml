name: Release

# Publishes both packages for a promoted vX.Y.Z tag. Only the promote
# workflow invokes this: reusable workflows sign with the CALLER's
# identity, so the npm and PyPI trusted publishers must reference
# promote.yml, and a hand-pushed tag would fail attestation anyway.

on:
  workflow_call:
    inputs:
      version:
        description: Version being released (X.Y.Z, no v prefix)
        required: true
        type: string

permissions:
  contents: read

jobs:
  verify-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refs/tags/v${{ inputs.version }}
      - name: Tag must match VERSION
        run: |
          tag="${{ inputs.version }}"
          version="$(cat VERSION)"
          if [ "$tag" != "$version" ]; then
            echo "tag v$tag does not match VERSION $version"
            exit 1
          fi

  npm:
    needs: verify-version
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        working-directory: typescript
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refs/tags/v${{ inputs.version }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm run build
      - run: npm test
      # npm only allows configuring a trusted publisher on a package that
      # already exists, so the first release bootstraps with a granular
      # token in the NPM_TOKEN secret; once the trusted publisher is set on
      # the package, delete the secret and this publishes via OIDC (which
      # needs npm 11.5+, newer than what Node 22 bundles).
      - name: Publish
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          npm install -g npm@latest
          if [ -n "$NPM_TOKEN" ]; then
            npm config set //registry.npmjs.org/:_authToken "$NPM_TOKEN"
          fi
          npm publish --provenance --access public

  pypi:
    needs: verify-version
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refs/tags/v${{ inputs.version }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m unittest discover -s tests
        working-directory: python
      - run: python -m pip install --quiet build && python -m build
        working-directory: python
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: python/dist/
